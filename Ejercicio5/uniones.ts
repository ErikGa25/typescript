// un valor puede tener dierentes tipos de datos

function convertir(valor: string | number) {
    if(typeof(valor) === 'string') {

    } else if(typeof(valor) === 'number') {

    }
}

convertir('mensaje')
convertir(250)